# 走水丸 機能仕様書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| アプリ名 | 走水丸 〜横須賀ボートフィッシングナビ〜 |
| パッケージ名 | `tk.horiuchi.hashirimizumaru` |
| 対象バージョン | 1.0.0 |
| 対応OS | Android 8.0（API 26）以上 |
| データベース | Room Database Version 4 |
| 最終更新日 | 2026年8月1日 |

## 2. アプリ概要

横須賀市の大津港〜走水周辺で使用する、小型ボート釣り向けナビゲーションアプリ。

主な機能は次のとおり。

- 対象海域の地図・等深線・シーマーク表示
- 現在位置表示と追従
- ポイント管理
- 目的地ナビ
- 航跡記録・管理
- 写真を使用した釣果記録

本アプリは航海用電子海図ではなく、釣り支援を目的とする。

## 3. システム構成

| 分類 | 使用技術・サービス |
|---|---|
| UI | Kotlin / Jetpack Compose |
| 地図 | MapLibre |
| 背景地図 | OpenStreetMap |
| シーマーク | OpenSeaMap |
| 等深線 | 海しるAPI |
| 位置情報 | Google Play services Location |
| ローカルDB | Room |
| バックグラウンド処理 | 位置情報フォアグラウンドサービス |
| 写真情報 | Android Photo Picker / ExifInterface |

## 4. 画面構成

### 4.1 初回免責画面

初回起動時に表示する。

- アプリが航海用電子海図ではないことを表示する
- 地図・位置・水深・航路標識の正確性を保証しないことを表示する
- 「内容を理解して同意する」を押すまでメイン画面へ進めない
- 同意状態は端末内に保存し、2回目以降は表示しない

### 4.2 メイン画面

上部にアプリ名、下部に次の4タブを表示する。

| タブ | 内容 |
|---|---|
| 地図 | 地図、現在地、ポイント、航跡、ナビ情報 |
| ポイント | 登録済みポイントの一覧・編集・削除・ナビ開始 |
| 航跡 | 航跡セッションの一覧・編集・削除・地図表示 |
| 釣果 | 写真付き釣果の一覧・登録・編集・削除 |

右上メニューから次を表示できる。

- プライバシーポリシー
- バージョン情報

### 4.3 プライバシーポリシー画面

- `PRIVACY.ja.md`から生成してアプリに同梱したHTMLをアプリ内に表示する
- 外部ブラウザは起動しない
- JavaScriptは無効とする
- 戻る操作でメイン画面へ戻る

## 5. 地図機能

### 5.1 表示範囲

地図カメラを次の範囲内に制限する。

| 境界 | 値 |
|---|---:|
| 最小緯度 | 35.235 |
| 最大緯度 | 35.340 |
| 最小経度 | 139.650 |
| 最大経度 | 139.820 |

ズーム範囲は11.5〜18.0とする。

### 5.2 地図レイヤ

レイヤメニューで次を個別にON/OFFできる。

| レイヤ | 初期状態 | 表示 |
|---|---|---|
| シーマーク | ON | OpenSeaMapのブイ、灯台、航路標識 |
| 等深線 | ON | 20m、50m、100mを色分けした破線 |
| 航跡 | ON | 記録中および選択した過去航跡 |
| タイドグラフ | ON | 横須賀の現在推算潮位、潮回り、上げ下げ、次の満干潮 |

等深線の色は次のとおり。

| 水深 | 色 |
|---|---|
| 20m | 水色 |
| 50m | 黄色 |
| 100m | 赤系 |

### 5.3 等深線取得

1. 起動時に端末内の保存データを確認する。
2. 保存データがあれば直ちに表示する。
3. 保存データがなく、等深線がONの場合は海しるAPIから取得する。
4. 対象範囲に含まれる20m、50m、100m線だけを抽出する。
5. 抽出結果をアプリ専用領域へGeoJSONとして保存する。

レイヤメニューの「再取得」で強制更新できる。取得失敗時はエラーと再試行操作を表示する。

### 5.4 地図キャッシュ

- 背景地図とシーマークの事前一括ダウンロード機能は提供しない
- MapLibreが通常閲覧時に保存したキャッシュのみ再利用する
- オフライン時は、キャッシュに存在しない背景地図・シーマークを表示できない
- 等深線、ポイント、航跡、釣果は端末内データを使用する

### 5.5 スケールバー

- 地図左下に距離と両端付きの線を表示する
- コピーライト表示と重ならない小型サイズで表示する
- 緯度とズーム値から実距離を計算する
- `50 m`、`100 m`、`200 m`、`1 km`など、表示幅に適した値へ自動変更する
- ナビパネル表示時は重ならない位置へ移動する

### 5.6 カメラ状態

- 地図画面を離れる際の中心座標とズーム値を保持する
- 他タブから地図へ戻った場合は保持した状態を復元する
- ポイントまたは航跡の表示要求は1回だけ処理し、その後のタブ移動で再フォーカスしない

## 6. 現在位置

### 6.1 表示

- GPS受信後、現在地をオレンジ色のマーカーで表示する
- 初期状態では現在地追従をONとする
- 位置更新時は0.8秒のカメラ移動で現在地を中央へ移動する
- 地図を手動で移動すると追従をOFFにする
- 現在地ボタンで追従を再開する

### 6.2 更新条件

| モード | 更新間隔 | 最小移動距離 | 精度 |
|---|---:|---:|---|
| デバッグ | 1秒 | 0m | 高精度 |
| 通常・省電力ON | 15秒 | 30m | バランス |
| 通常・省電力OFF | 5秒 | 5m | 高精度 |

デバッグ版では受信した緯度・経度・精度を地図上に表示する。

## 7. ポイント機能

### 7.1 データ項目

| 項目 | 必須 | 内容 |
|---|---|---|
| 名前 | 必須 | ポイント名 |
| メモ | 任意 | 補足 |
| 緯度 | 必須 | 10進表記 |
| 経度 | 必須 | 10進表記 |
| 作成日時 | 自動 | 初回保存日時 |
| 更新日時 | 自動 | 最終保存日時 |

水深項目は持たない。

### 7.2 登録

次の方法で登録できる。

- 地図画面の「ポイント」で現在地を登録
- 地図を長押しして押下地点を登録
- ポイント画面の追加ボタンから登録

名前と有効な緯度・経度が入力されるまで保存できない。

### 7.3 一覧操作

- カードタップ：地図へ切り替え、対象ポイントを中央表示する
- ナビボタン：地図へ切り替え、ナビ確認状態にする
- 編集ボタン：名前、座標、メモを編集する
- 削除ボタン：確認後に削除する

地図上ではポイントをマーカーと名前で表示する。ナビ対象は黄色で強調する。

## 8. ナビゲーション

### 8.1 ナビ確認

ポイント画面のナビボタンでは、ナビを即時開始しない。

1. 地図画面へ切り替える。
2. ゴールを強調して中央表示する。
3. 確認パネルにポイント名、距離、方位、方角を表示する。
4. 「ナビ開始」または「キャンセル」を選択する。

現在地未取得時は開始ボタンを無効化する。現在地がゴールの30m以内の場合も開始せず、「到着範囲内」と表示する。

キャンセル時はナビを開始せず、ポイント画面へ戻る。

### 8.2 ナビ開始

「ナビ開始」押下時に次を行う。

- 押下時の現在地をスタート地点として保持する
- 目的地を設定する
- スタート地点とゴールを黄色の破線で結ぶ
- 現在地追従をONにする
- 航跡記録がOFFの場合は、ナビ連動航跡を新規作成する
- 常駐通知を表示する
- 開始メッセージを表示する

### 8.3 ナビ中

画面下部に次を表示する。

- 目的地名
- 現在地からの残距離
- 方位角
- 8方位の方角
- 終了ボタン

残距離と方位は現在地更新ごとに再計算する。

### 8.4 ナビ終了

終了方法は次の3種類。

- 地図画面の「終了」
- 常駐通知の「ナビ終了」
- ゴール30m以内への到達

終了時に次を行う。

- 目的地を解除する
- ナビ直線を消去する
- 到着時はバイブレーションと通知を行う
- ナビによって開始した航跡記録を終了する
- ナビ開始前から手動記録中だった航跡は継続する

ナビ中は地図画面の航跡記録ボタンから記録を停止できない。先にナビを終了する。

## 9. 航跡機能

### 9.1 手動記録

- 地図画面の航跡ボタンで開始・終了する
- 開始時に航跡セッションを新規作成する
- デフォルト名は開始日時の`yyyy/MM/dd HH:mm`とする
- 15秒以上経過した位置をRoomへ保存する
- 記録中の航跡は黄色の太い実線で表示する
- 保存済みの過去航跡は水色の実線で表示する

### 9.2 バックグラウンド記録

- 記録中は`location`タイプのフォアグラウンドサービスを起動する
- 画面OFF、ホーム画面、他アプリ表示中も位置取得と保存を継続する
- 常駐通知から記録またはナビを終了できる
- サービスが再生成された場合は保存済み状態を復元する
- アプリを再表示した場合は記録状態と目的地を復元する

### 9.3 未終了航跡

サービスの保存状態がなく、Roomに終了日時未設定の航跡がある場合は、起動時に確認ダイアログを表示する。

- 「記録を続ける」：同じ航跡セッションで記録を再開する
- 「記録を終了」：現在時刻を終了日時として保存する

### 9.4 航跡一覧

各航跡に次を表示する。

- 名前
- 記録状態
- 開始・終了日時
- 記録時間
- 移動距離
- 記録点数
- メモ

操作は次のとおり。

- カードまたは地図ボタン：航跡全体が収まる状態で地図表示
- 編集：名前とメモを変更
- 削除：航跡セッションと所属する全座標を削除
- 記録中の航跡：一覧から終了可能、削除不可

## 10. 釣果機能

### 10.1 新規登録フロー

1. 「写真から記録」を押す。
2. 必要に応じて写真位置情報権限の説明を表示する。
3. Android Photo Pickerで写真を1枚選択する。
4. EXIFを読み取り、写真をアプリ専用領域へコピーする。
5. 入力画面で内容を確認・修正する。
6. 保存する。

写真選択のキャンセル時は登録画面を開かない。

### 10.2 EXIFと初期値

| 項目 | 第1候補 | フォールバック |
|---|---|---|
| 撮影日時 | EXIF撮影日時 | 現在時刻 |
| 緯度・経度 | EXIF GPS | アプリの現在地 |

写真位置情報権限を許可しない場合も写真選択を継続できる。その場合、座標には現在地を使用する。

### 10.3 写真保存

- EXIFの向き情報に従って回転・反転する
- 長辺が1800pxを超える場合は縮小する
- JPEG品質88%で保存する
- 保存先はアプリ専用領域の`catch_photos`とする
- ファイル名にはUUIDを使用する
- Roomには`catch_photos/<UUID>.jpg`形式の相対パスを保存する
- 元写真を変更・削除しない
- 入力キャンセル時は作成済みコピーを削除する

### 10.4 入力項目

| 項目 | 必須 | 編集 |
|---|---|---|
| 写真 | 必須 | 登録後の差し替え不可 |
| 撮影日時 | 必須 | 可 |
| 緯度 | 必須 | 可 |
| 経度 | 必須 | 可 |
| サイズ（cm） | 任意 | 可 |
| メモ | 任意 | 可 |

魚種項目は持たない。

### 10.5 入力画面

- 写真、日時、座標の取得元を表示する
- 緯度は-90〜90、経度は-180〜180の範囲で検証する
- サイズは数値として検証する
- キーボード表示中も入力領域をスクロールできる
- キーボード表示中も「キャンセル」「保存」を固定領域に表示する
- メモ欄では改行入力を許可する

### 10.6 一覧・編集・削除

- 写真を中心としたカード形式で一覧表示する
- 撮影日時、サイズ、座標、メモを表示する
- 編集では写真以外の項目を変更できる
- 削除前に確認する
- 削除時はRoomデータとアプリ専用領域の写真を削除する
- 旧バージョンから移行した写真なしデータには代替アイコンを表示する

## 11. 権限

### 11.1 権限一覧

| 権限 | 要求タイミング | 用途 |
|---|---|---|
| `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` | 初回起動時 | 現在地、ナビ、航跡、釣果位置 |
| `POST_NOTIFICATIONS` | 初回のナビ・航跡開始直前 | 常駐通知、到着通知 |
| `ACCESS_MEDIA_LOCATION` | 釣果写真選択直前 | 選択写真のEXIF GPS取得 |
| `FOREGROUND_SERVICE` | Manifest宣言 | バックグラウンド継続 |
| `FOREGROUND_SERVICE_LOCATION` | Manifest宣言 | 位置情報サービス |
| `VIBRATE` | Manifest宣言 | 到着時の振動 |
| `INTERNET` | Manifest宣言 | 地図・シーマーク・等深線取得 |

### 11.2 権限動作

- 位置情報を拒否した場合、現在地依存機能を利用できない
- 位置情報拒否後はアプリ設定画面への案内を表示する
- 設定画面で許可してアプリへ戻った場合は位置取得を開始する
- 通知権限の前に利用目的を説明する
- 通知を拒否してもナビ・航跡記録は開始する
- 写真位置情報は「許可して選択」「許可せず選択」を選べる
- 写真ライブラリ全体の読み取り権限は要求しない
- `ACCESS_BACKGROUND_LOCATION`は要求しない

## 12. データ仕様

### 12.1 `waypoints`

| 列 | 型 | 内容 |
|---|---|---|
| `id` | Long | 主キー、自動採番 |
| `name` | String | 名前 |
| `memo` | String | メモ |
| `latitude` | Double | 緯度 |
| `longitude` | Double | 経度 |
| `created` | Long | 作成日時 |
| `updated` | Long | 更新日時 |

### 12.2 `catches`

| 列 | 型 | 内容 |
|---|---|---|
| `id` | Long | 主キー、自動採番 |
| `time` | Long | 撮影日時 |
| `latitude` | Double | 緯度 |
| `longitude` | Double | 経度 |
| `size` | Double? | サイズ |
| `photoUri` | String? | 写真の相対パス |
| `memo` | String | メモ |

### 12.3 `track_sessions`

| 列 | 型 | 内容 |
|---|---|---|
| `id` | Long | 主キー、自動採番 |
| `name` | String | 航跡名 |
| `memo` | String | メモ |
| `startedAt` | Long | 開始日時 |
| `endedAt` | Long? | 終了日時 |
| `startedByNavigation` | Boolean | ナビ連動開始フラグ |

### 12.4 `tracks`

| 列 | 型 | 内容 |
|---|---|---|
| `id` | Long | 主キー、自動採番 |
| `sessionId` | Long | 航跡セッションID |
| `time` | Long | 記録日時 |
| `latitude` | Double | 緯度 |
| `longitude` | Double | 経度 |

## 13. Roomマイグレーション

| バージョン | 変更 |
|---|---|
| 1 → 2 | ポイントから水深列を削除し、既存ポイントを維持 |
| 2 → 3 | 航跡セッションを追加し、既存航跡を「以前の航跡」へ移行 |
| 3 → 4 | 釣果から魚種列を削除し、既存釣果を維持 |

マイグレーションに失敗した場合の破壊的再作成は行わない。

## 14. 通知

### 14.1 ナビ・航跡常駐通知

| 状態 | タイトル | 操作 |
|---|---|---|
| 航跡のみ | 航跡を記録中 | 記録終了 |
| ナビ中 | ナビ中 | ナビ終了 |

通知タップでアプリを開く。通知はサービス停止まで消去不可とする。

### 14.2 到着通知

- タイトル：目的地に到着しました
- 本文：目的地名と30m以内に入ったことを表示
- 重要度：高
- バイブレーション：800ms
- タップ後は消去可能

## 15. エラー・例外処理

| 状況 | 動作 |
|---|---|
| 位置情報未取得 | 現在地依存ボタンを無効化、または取得中表示 |
| 位置権限拒否 | 説明とアプリ設定への導線を表示 |
| 通知権限拒否 | 設定で許可できる旨を表示し、処理は継続 |
| 等深線APIキー未設定 | `local.properties`の設定不足を表示 |
| 等深線取得失敗 | エラーと再試行操作を表示。保存データがあれば使用 |
| 写真読込失敗 | エラーダイアログを表示し、登録しない |
| 写真にEXIF日時なし | 現在時刻を使用 |
| 写真にEXIF GPSなし | 現在地を使用 |
| 写真保存失敗 | 作成途中ファイルを削除し、登録しない |
| 未終了航跡 | 継続または終了を選択 |

## 16. 非機能仕様

### 16.1 UI

- ダークテーマを使用する
- 日光下を考慮した高コントラスト配色とする
- ボート上で押しやすい大きさのボタンを使用する
- 主要操作は片手で完了できる構成とする

### 16.2 省電力

- 通常時は15秒・30mを基準に位置更新する
- 必要時のみ高精度モードを使用する
- 不要なセンサーと通信を使用しない
- 地図アニメーションを必要最小限とする

### 16.3 データ・プライバシー

- ポイント、航跡、釣果、写真は端末内に保存する
- Androidの自動バックアップおよび端末間データ転送を無効とする
- アンインストール時に端末内のアプリデータを削除する。事前に手動バックアップした場合は再インストール後に復元できる
- アカウント登録、クラウド同期、開発者サーバーへの送信は行わない
- 写真は選択した1枚だけへアクセスする
- 写真のEXIF解析と縮小処理は端末内で行う

### 16.4 安全

- 初回起動時に免責事項への同意を必須とする
- 地図画面に海しるAPIの免責表記を表示する
- 本アプリだけで航行判断を行わないことを明記する

## 17. 追加機能・追加予定機能

17.2および17.3は将来バージョンでの追加を予定する。17.1の簡易タイドグラフと17.4の手動バックアップ・復元は実装済みとする。予定機能で使用するデータ提供元、更新間隔、通信失敗時の動作は実装時に決定する。

### 17.1 タイドグラフ

- 海上保安庁が公開する横須賀験潮所の主要8分潮の調和定数を使用し、端末内で推算する
- 年間潮位表の同梱、API通信およびデータのダウンロードは行わない
- 地図画面右上の操作ボタン群の下へ、半透明の小型パネルとして表示する
- 現在前後約6時間の曲線および現在時刻マーカーを表示する
- 次の満潮または干潮の時刻を表示する
- 月齢の簡易計算から、大潮、中潮、小潮、長潮または若潮を表示する
- 小型パネルには潮回り、次の満潮・干潮時刻およびグラフだけを表示する
- GPSデバッグ情報パネルと同程度の横幅とし、潮回りと満干潮時刻は2段で表示する
- 表示は1分ごとに更新する
- パネルのタップ操作および詳細画面は設けない
- レイヤ設定に表示スイッチを設け、初期状態はONとする
- レイヤ設定パネルは地図上の操作ボタンおよびタイドグラフより前面に表示する
- 潮位は気圧、風、波浪などを反映しない推算値であり、実際の潮位とは異なる場合がある
- 潮位表示は潮流の方向および速さを示すものではなく、出航・航行判断には使用しない

### 17.2 天気予報

- 対象海域の天気、気温、降水確率を表示する
- 風向、平均風速、最大瞬間風速を表示する
- 時間帯別の予報を確認できるようにする
- 予報の対象地点、発表時刻、更新時刻を表示する
- 強風など安全に影響する情報を識別しやすく表示する
- 予報情報だけで出航・航行を判断しないことを明記する

小型ボートでは風向と風速が安全性や帰航の難易度に直接影響するため、風情報は天気予報機能の必須項目として扱う。

### 17.3 ポイント・航跡のインポート／エクスポート

- 登録済みポイントをファイルへエクスポートできるようにする
- 航跡セッションと所属する座標をファイルへエクスポートできるようにする
- エクスポートしたファイルからポイントと航跡をインポートできるようにする
- Androidのファイル選択・保存画面を使用し、利用者が保存先と読込元を選択できるようにする
- インポート前にファイル形式と必須項目を検証する
- 同一データが存在する場合の追加、上書き、スキップ方法は実装時に決定する
- 部分的な読込失敗で既存データが破損しないよう、トランザクション処理を行う
- 入出力するファイル形式とバージョン管理方法は実装時に決定する

### 17.4 手動バックアップ・復元

#### 保存・読込方式

- Android標準のファイル保存画面とファイル選択画面を使用する
- 利用者がGoogle Drive、端末内ストレージなどの保存先を選択する
- Google Drive APIとの直接連携、Googleアカウントへのログイン、自動同期は行わない
- ストレージ全体を読み取る権限は要求しない
- バックアップは利用者が明示的に操作した場合だけ実行する

#### バックアップ対象

- ポイント
- 航跡セッションと所属する全座標
- 釣果情報
- アプリ専用領域に保存した釣果写真

免責事項への同意状態、現在のナビ状態、記録中状態、海しるAPIキー、
等深線キャッシュ、背景地図キャッシュおよびシーマークキャッシュは対象外とする。

#### ファイル仕様

- 拡張子は `.hmbak` とする
- 既定ファイル名は `hashirimizumaru_yyyyMMdd_HHmm.hmbak` とする
- ファイル内部はZIP形式とし、バージョン付きの `manifest.json`、
  各データのJSONおよび釣果写真を格納する
- Roomのデータベースファイルを直接コピーしない
- バックアップには位置情報と写真が含まれることを保存前に表示する

#### バックアップ操作

1. バックアップ対象の件数と推定容量を表示する。
2. 利用者が保存先とファイル名を選択する。
3. 一時ファイルへバックアップを生成する。
4. 選択された保存先へ書き込む。
5. 成功時に保存件数、ファイル名および実行日時を表示する。

航跡記録中またはナビ中はバックアップを開始できない。キャンセルまたは失敗時は、
作成途中のデータを破棄し、端末内の既存データを変更しない。

#### 復元操作

1. 利用者がバックアップファイルを選択する。
2. ファイル形式、バージョン、必須項目、座標、写真および展開後容量を検証する。
3. 作成日時と復元対象件数を表示する。
4. 現在のデータを置き換えることへの確認を求める。
5. 検証成功後、Roomトランザクションと一時領域を使用して復元する。
6. 成功後に画面のデータを再読み込みする。

初期実装の復元方式は完全置換のみとする。追加、マージおよび重複判定は、
ポイント・航跡のインポート機能として別途実装する。復元失敗時は現在のデータを維持する。

#### アンインストールとの関係

- Androidの自動バックアップおよび端末間自動転送は無効のままとする
- アンインストール時にデータ保持の選択肢を追加しない
- アンインストール前の手動バックアップは利用者の操作と責任で行う
- アンインストールすると、バックアップファイル以外のアプリ内データは削除される

## 18. その他のVer.1.0対象外機能

- 背景地図・シーマークの事前オフラインダウンロード
- GPX入出力
- 潮流表示
- 魚探ログ連携
- 複数ポイントルート
- クラウド同期
- ボート速度表示
- 水温記録
- 航跡共有
