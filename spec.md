# ボート釣りナビアプリ 要求仕様書

| 項目 | 内容 |
|---|---|
| アプリ名 | 走水丸 〜横須賀ボートフィッシングナビ〜 |
| パッケージ名 | `tk.horiuchi.hashirimizumaru` |

## 1. コンセプト

横須賀市の大津港〜走水周辺を対象とした、小型ボート釣り専用ナビゲーションアプリ。

一般的な海図アプリのような多機能性ではなく、次の用途に特化する。

- よく行くポイントの管理
- 水深の確認
- 釣果の記録
- ボートの移動履歴の記録

「釣り人が一日中使える軽量・省電力アプリ」を目指す。

## 2. 想定ユーザー

- 小型ボート所有者
- レンタルボート利用者
- GPS魚探はあるが、スマートフォンでもポイントを管理したい人
- 主に大津港〜走水周辺で釣りをする人

## 3. 開発環境

- Android Studio
- Kotlin
- Jetpack Compose
- Room Database
- MapLibre

## 4. 対象エリア

アプリは次の地域を中心とした狭い範囲に限定する。

- 大津港
- 観音崎
- 走水
- 猿島周辺

日本全国を自由に閲覧する用途とはせず、地図カメラも対象範囲外へ移動できないようにする。

## 5. 優先順位

1. シンプル
2. 高速
3. 軽量
4. 省電力
5. 通信量の抑制

一般的な海図アプリとの差別化として「必要最低限」を徹底する。

## 6. 地図

### ベースマップ

- シンプルな地図を使用する
- 陸地の表示は最低限とし、海を中心に表示する
- マップ上に現在の縮尺を示すスケールバーを表示する

### 等深線

- 海しるAPI等を利用する
- 20m、50m、100mの等深線を表示する
- 航跡と区別できるよう破線で表示する
- デフォルトで表示する
- 表示のON/OFFを切り替えられる
- 取得したデータを端末内にキャッシュする

### シーマーク

OpenSeaMapを利用して次の情報を表示する。

- ブイ
- 灯台
- 航路標識

表示のON/OFFを切り替えられる。

### 潮流

東京湾の潮流表示は将来対応とする。

## 7. 現在位置

- GPSで取得する
- マップ上に現在地マーカーを表示する
- 通常は現在地を画面中央に保って地図を追従させる
- 地図を手動操作すると追従を解除する
- 現在地ボタンで追従を再開する

## 8. ポイント

### 登録項目

- 名前
- メモ
- 緯度
- 経度

### 操作

- 登録
- 編集
- 削除
- 一覧表示
- 一覧から地図上の位置を表示
- 地図の長押し地点を登録
- 現在地を登録

ポイント名はマップ上にも表示する。

## 9. ナビゲーション

- 目的地を1つ選択する
- ポイント画面からナビを選択すると、マップ上でゴールをプレビューする
- 確認パネルに距離と方位を表示する
- 「ナビ開始」を押した時点の現在地をスタート地点とする
- スタート地点とゴール地点を直線で結ぶ
- ナビ開始後は現在地追従へ戻る
- ナビ開始と同時に航跡記録を自動開始する
- ナビ終了時に、ナビによって開始した航跡記録も終了する
- 手動で開始済みの航跡記録は、ナビ終了後も継続する

### ナビ中の表示

- 目的地名
- 目的地までの距離
- 方位
- 方角
- スタート地点とゴール地点を結ぶ直線

### 到着判定

ゴールの30m以内に入った場合は次の処理を行う。

- バイブレーション
- 到着通知
- ナビの自動終了
- ナビ直線の消去
- ナビによって開始した航跡記録の終了

## 10. GPS航跡

### 記録

- 15秒間隔を基本として時刻・緯度・経度を保存する
- 航跡を釣行単位のセッションとして管理する
- デフォルトの航跡名は`yyyy/MM/dd HH:mm`形式とする
- 記録中の航跡をマップ上に実線で表示する
- 過去の航跡も選択して表示できる

### 航跡管理

- 一覧表示
- 名前・メモの編集
- 記録日時、記録時間、距離、点数の表示
- 地図上への表示
- 個別削除
- 未終了航跡の継続または終了

### バックグラウンド動作

- ナビ中または航跡記録中は位置情報フォアグラウンドサービスを使用する
- 画面OFF、ホーム画面表示、他アプリ使用中も航跡保存と到着判定を継続する
- 動作中は常駐通知を表示する
- 通知からナビまたは航跡記録を終了できる
- バックグラウンド位置情報権限は要求しない

GPX入出力は将来対応とする。

## 11. 釣果記録

### 登録項目

- 写真（必須）
- 撮影日時
- 緯度・経度
- サイズ（任意）
- メモ（任意）

### 写真処理

- Androidの写真選択画面から写真を選択する
- EXIFから撮影日時と緯度・経度を読み取る
- EXIFに日時がない場合は現在時刻を使用する
- EXIFに位置がない場合は現在地を使用する
- 写真の向きを補正する
- 長辺1800px、JPEG品質88%に縮小する
- 縮小写真をアプリ専用領域へ保存する
- Roomには写真本体ではなく相対パスを保存する
- 入力キャンセル時は一時保存した写真を削除する
- 釣果削除時は保存した写真も削除する

### 操作

- 写真中心の一覧表示
- 撮影日時、緯度、経度、サイズ、メモの編集
- 削除

魚種は入力項目として持たない。

## 12. データ保存

Room Databaseを使用する。

### `waypoints`

| 列 | 内容 |
|---|---|
| `id` | ID |
| `name` | 名前 |
| `memo` | メモ |
| `latitude` | 緯度 |
| `longitude` | 経度 |
| `created` | 作成日時 |
| `updated` | 更新日時 |

### `catches`

| 列 | 内容 |
|---|---|
| `id` | ID |
| `time` | 撮影日時 |
| `latitude` | 緯度 |
| `longitude` | 経度 |
| `size` | サイズ |
| `photoUri` | アプリ専用領域内の写真相対パス |
| `memo` | メモ |

### `track_sessions`

| 列 | 内容 |
|---|---|
| `id` | ID |
| `name` | 航跡名 |
| `memo` | メモ |
| `startedAt` | 開始日時 |
| `endedAt` | 終了日時 |
| `startedByNavigation` | ナビ連動で開始したか |

### `tracks`

| 列 | 内容 |
|---|---|
| `id` | ID |
| `sessionId` | 航跡セッションID |
| `time` | 記録日時 |
| `latitude` | 緯度 |
| `longitude` | 経度 |

## 13. 地図キャッシュ

- 背景地図およびシーマークの事前一括ダウンロード機能は提供しない
- 通常閲覧時にMapLibreが保存したキャッシュは、提供元の条件に従って再利用する
- 等深線、ポイント、航跡および釣果は端末内に保存する

## 14. 省電力設計

### GPS

通常時の目安は次のとおり。

- 15秒更新
- 30m移動

デバッグ時および必要時は高精度更新を使用する。

### ログ保存

- 航跡記録中は15秒間隔を基本としてRoomへ保存する
- 画面のライフサイクルではなくフォアグラウンドサービスで処理する

### 地図描画

- 不要なアニメーションを避ける
- 停止中と移動中で必要以上に描画負荷を上げない

### センサー

コンパス等は必要な場合のみ有効にする。

### 通信

- 地図タイルの通常キャッシュを利用する
- 不要な通信を行わない

### 画面

- 黒基調
- 高コントラスト

## 15. UI

ボート上で使用することを前提とする。

- ボタンを大きくする
- 操作回数を少なくする
- 日光下でも見やすくする
- 片手で操作できるようにする
- キーボード表示中も入力欄をスクロール可能にする
- ダイアログの保存・キャンセル操作を固定領域に表示する

## 16. 権限

| 権限 | 要求タイミング・用途 |
|---|---|
| 正確／おおよその位置情報 | 初回起動時。現在地、ナビ、航跡記録に使用 |
| 通知 | 初めてナビまたは航跡記録を開始する直前。常駐通知と到着通知に使用 |
| 選択した写真の位置情報 | 釣果写真を選択する直前。EXIF位置の取得に使用 |
| フォアグラウンドサービス | Manifestで宣言。バックグラウンドでのナビ・航跡記録に使用 |
| バイブレーション | 目的地への到着通知に使用 |
| インターネット | 地図、シーマーク、等深線の取得に使用 |

### 権限設計

- 位置情報を拒否した場合はアプリ設定画面への案内を表示する
- 通知権限は利用目的を説明してから要求する
- 通知を拒否してもナビ・航跡記録は開始できる
- 写真の位置情報は「許可して選択」「許可せず選択」を選べる
- 写真ライブラリ全体の読み取り権限は要求しない
- `ACCESS_BACKGROUND_LOCATION`は要求しない

## 17. 安全に関する注意

本アプリは航海用電子海図ではなく、釣り支援アプリである。

- 航行判断には利用しない
- 法令で求められる海図・航海計器・目視確認を使用する
- 初回起動時に免責事項への同意を求める

## 18. MVP（初版リリース対象）

- 対象エリア（大津港〜走水）の地図表示
- 現在位置表示・追従
- 等深線表示
- シーマーク表示
- ポイント登録・編集・削除
- 目的地ナビ
- バックグラウンドGPS航跡記録
- 航跡管理
- 写真を使用した釣果記録
- 省電力モード
- 免責事項表示
- プライバシーポリシー表示
- バージョン情報表示

## 19. 将来追加予定

- ポイントデータのインポート／エクスポート
- 航跡データのインポート／エクスポート
- GPX入出力
- 潮流表示
- 天気
- 風向・風速
- 魚探ログ連携
- 複数ポイントルート
- クラウド同期
- ボート速度表示
- 水温記録
- 航跡共有

## 20. 開発方針

作者自身が実際の釣行で使用しながら改善を重ねることを前提とする。

重視する設計思想は次の3つ。

- **軽い（Lightweight）**：不要な機能を削ぎ落とし、軽快に動作する
- **速い（Fast）**：必要な情報へすぐアクセスでき、操作回数を最小限にする
- **一日使える（Battery Friendly）**：長時間釣行を想定してGPS・描画・通信を最適化する

「高機能」よりも「現場で本当に使いやすいこと」を最優先とする。
